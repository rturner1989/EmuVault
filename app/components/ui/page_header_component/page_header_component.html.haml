.flex.items-start.justify-between.gap-4.mb-6
  .min-w-0
    %h1.text-2xl.font-bold.truncate
      = @title
    - if @subtitle
      %p.mt-1.text-sm.text-muted.opacity-
        = @subtitle
  - if actions.any?
    .flex.items-center.gap-2.shrink-0
      - actions.each do |action|
        = action

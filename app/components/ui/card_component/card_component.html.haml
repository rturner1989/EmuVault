%div{ class: @class, **@html_options }
  - if header?
    %div{ class: "shrink-0 #{@padding}" }
      = header

  - if body?
    %div{ class: body_class }
      = body
  - elsif content_class
    %div{ class: content_class }
      = content
  - else
    = content

  - if footer?
    %div{ class: "shrink-0 #{@padding}" }
      = footer
  - elsif footer_actions?
    %div{ class: "shrink-0 flex gap-3 #{@padding}" }
      - footer_actions.each do |action|
        = action

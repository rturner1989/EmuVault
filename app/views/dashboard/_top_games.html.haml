.rounded-lg.bg-base-100.border.border-base-300
  .px-5.py-4.border-b.border-base-300
    %h2.text-sm.font-semibold
      = t(".title")

  - if top_games.empty?
    .px-5.py-6.text-center.text-sm.text-muted
      = t(".empty")
  - else
    %ul.divide-y.divide-base-300
      - top_games.each do |game|
        %li.px-5.py-3.flex.items-center.justify-between.gap-2
          = render ::UI::ActionComponent.new(href: game_path(game[:id]),
            variant: :link_primary,
            class: "text-sm truncate") do
            = game[:title]
          .text-xs.text-muted.shrink-0
            = t(".sync_count", count: game[:count])

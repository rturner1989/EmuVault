%span{ "data-notification-badge": "", class: "absolute -top-1 -right-1 min-w-[1rem] h-4 bg-drac-red text-white text-xs rounded-full flex items-center justify-center leading-none px-0.5 pointer-events-none#{ ' hidden' if count.zero? }" }
  - unless count.zero?
    = count

DB_HOST=postgres
DB_USERNAME=emuvault
DB_PASSWORD=emuvault
DB_NAME=emu_vault_development

REDIS_URL=redis://redis:6379/0

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Activity log retention — SyncEvent records older than this many days are pruned nightly (default: 90)
ACTIVITY_RETENTION_DAYS=90

# Used by rake screenshots task to log in and capture pages
SCREENSHOT_PASSWORD=
